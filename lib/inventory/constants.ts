/** Default warehouse when multi-warehouse is not yet enabled in UI. */
export const DEFAULT_WAREHOUSE_ID = "default";

export const DEFAULT_WAREHOUSE_CODE = "MAIN";

export const DEFAULT_WAREHOUSE_NAME = "Main warehouse";
